<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Checkout"></activity>
        <activity android:name=".ShoppingCart" />
        <activity android:name=".ViewStockItemAdmin" />
        <activity android:name=".ViewStockItemCustomer" />
        <activity android:name=".CustomerViewProducts" />
        <activity android:name=".CustomerView" />
        <activity android:name=".ViewCustomers" />
        <activity android:name=".AdminViewStock" />
        <activity android:name=".CustomerHome" />
        <activity android:name=".AdminHome" />
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>